{
  "name": "lineer-chat-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers chat application with Durable Objects",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "durable-objects",
    "chat",
    "websocket"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^4.35.0",
    "typescript": "^5.0.0"
  }
}